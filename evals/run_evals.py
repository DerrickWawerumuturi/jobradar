"""Eval harness — your differentiator (Day 9).
Almost nobody in a junior portfolio does this. ~15-20 Q/A pairs.

TODO:
- load evals/eval_set.jsonl
- for each: run rag.answer, check (a) retrieval pulled the expected posting,
  (b) answer contains expected content
- print + log retrieval hit-rate and answer-accuracy scores
"""
